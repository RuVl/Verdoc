# Local-dev infra: postgres only, published to localhost:5432 for the host backend/frontend.
# No nginx/backend containers here (sidesteps rootless-podman 80/443). See `make dev-*`.
# Shares the verdoc_postgres volume with the prod stack - never run both postgres at once.

volumes:
  postgres_volume:
    name: verdoc_postgres

services:
  postgres:
    build:
      context: ./postgres
      dockerfile: Dockerfile
    restart: unless-stopped
    volumes:
      - postgres_volume:/var/lib/postgresql/data
    env_file: postgres/.env
    ports:
      - "5432:5432"
    healthcheck:
      test: pg_isready -U $$POSTGRES_USER
      interval: 15s
      timeout: 5s
      retries: 3
