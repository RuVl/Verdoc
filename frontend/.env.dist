# nginx. Both are substituted into the site templates: the same containers serve a primary domain
# and a mirror, and each needs its certificate at /etc/ssl/<domain>.{crt,key}.
DOMAIN=mysite.com
MIRROR_DOMAIN=mymirror.com

# vite
VITE_API_URL=https://mysite.com/api
